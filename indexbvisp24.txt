<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Biblioteca Virtual ISP24</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet" />
  <style>
    :root {
      --primary-color: #1976d2;
      --background-color: #f4f6f8;
      --card-bg: #fff;
      --text-color: #2c3e50;
      --shadow: rgba(0, 0, 0, 0.1);
    }

    * {
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', sans-serif;
      margin: 0;
      background-color: var(--background-color);
      color: var(--text-color);
    }

    header {
      background-color: var(--primary-color);
      color: white;
      padding: 20px;
      text-align: center;
      box-shadow: 0 2px 8px var(--shadow);
    }

    h1 {
      margin: 0;
      font-size: 2rem;
    }

    .container {
      display: flex;
      max-width: 1200px;
      margin: 40px auto;
      gap: 20px;
      padding: 0 20px;
    }

    .filters {
      flex: 1;
      max-width: 250px;
      margin: 0 auto;
      background: var(--card-bg);
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 4px 12px var(--shadow);
      text-align: center;
    }

    .filters label {
      display: block;
      margin: 15px 0 5px;
      font-weight: 600;
    }

    .filters select {
      width: 100%;
      padding: 8px;
      border-radius: 6px;
      border: 1px solid #ccc;
      background-color: #f9f9f9;
      font-size: 0.9rem;
    }

    .results {
      flex: 3;
      background: var(--card-bg);
      padding: 25px;
      border-radius: 10px;
      box-shadow: 0 4px 12px var(--shadow);
    }

    .results h2 {
      color: var(--primary-color);
    }

    ul#fileList {
      list-style: none;
      padding: 0;
    }

    ul#fileList li {
      background: #e3f2fd;
      margin-bottom: 10px;
      padding: 12px 15px;
      border-radius: 8px;
    }

    .footer {
      max-width: 1200px;
      margin: 40px auto;
      padding: 20px;
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 4px 10px var(--shadow);
      font-size: 0.9rem;
    }

    .footer h3 {
      margin-top: 0;
      color: var(--primary-color);
    }

    .donar {
      margin-top: 20px;
      background: #e8f5e9;
      padding: 15px;
      border-left: 5px solid #43a047;
      border-radius: 8px;
    }

    .donar strong {
      color: #2e7d32;
    }

    @media (max-width: 768px) {
      .container {
        flex-direction: column;
      }

      .filters {
        max-width: 100%;
        text-align: left;
      }

      .results {
        margin-top: 20px;
      }
    }
  </style>
</head>
<body>
  <header>
    <h1>Biblioteca Virtual ISP24</h1>
  </header>

  <div class="container">
    <div class="filters">
      <h2>Filtrar</h2>
      <label for="carrera">Carrera:</label>
      <select id="carrera" onchange="updateMateria()"></select>

      <label for="ciclolectivo">Ciclo lectivo:</label>
      <select id="ciclolectivo" onchange="updateYear()"></select>

      <label for="añocarrera">Año de carrera:</label>
      <select id="añocarrera" onchange="updateMateria()"></select>

      <label for="materia">Materia:</label>
      <select id="materia" onchange="fetchFiles()"></select>
    </div>

    <div class="results">
      <h2>Resultados</h2>
      <ul id="fileList"></ul>
    </div>
  </div>

<div class="footer">
  <h3>Términos de uso</h3>
  <p>Esta biblioteca virtual es de acceso libre y gratuito. Todo el contenido está destinado exclusivamente para fines educativos y no comerciales. El uso responsable de los materiales es fundamental para mantener este recurso disponible para toda la comunidad educativa.</p>

  <div class="donar">
    <h4>¿Querés colaborar?</h4>
    <p><strong>Profesores:</strong> ¿Quieren compartir sus archivos? Pueden enviarlos al número del administrador de la biblioteca virtual: <strong>1123456789</strong></p>
    <p><strong>Alumnos:</strong> ¿Problemas con los archivos? Enviá tus consultas a <a href="mailto:soporte.bibliovirtualisp24@gmail.com">soporte.bibliovirtualisp24@gmail.com</a></p>
    <hr />
    <p>También podés ayudarnos con una donación voluntaria para mantener este servicio.</p>
    <p><strong>Alias para donar:</strong> <code>biblio.isp24</code></p>
  </div>
</div>

  <script>
    const SHEET_ID = '1tTfCtb0SJMPtB3diWgYDCV4nfBqWKeQPKdXNM7e31rA';
    const SHEET_NAME = 'Hoja1';

    async function fetchSpreadsheet() {
      const response = await fetch(`https://sheets.googleapis.com/v4/spreadsheets/${SHEET_ID}/values/${SHEET_NAME}?key=YOUR_API_KEY`);
      const data = await response.json();
      const rows = data.values;

      const carreraSet = new Set();
      const ciclolectivoSet = new Set();
      const añocarreraSet = new Set();
      const materiaSet = new Set();

      rows.forEach(row => {
        carreraSet.add(row[0]);
        ciclolectivoSet.add(row[1]);
        añocarreraSet.add(row[2]);
        materiaSet.add(row[3]);
      });

      const carreraSelect = document.getElementById('carrera');
      carreraSet.forEach(c => {
        const option = document.createElement('option');
        option.text = c;
        option.value = c;
        carreraSelect.add(option);
      });

      const ciclolectivoSelect = document.getElementById('ciclolectivo');
      ciclolectivoSet.forEach(cl => {
        const option = document.createElement('option');
        option.text = cl;
        option.value = cl;
        ciclolectivoSelect.add(option);
      });

      const añocarreraSelect = document.getElementById('añocarrera');
      añocarreraSet.forEach(ac => {
        const option = document.createElement('option');
        option.text = ac;
        option.value = ac;
        añocarreraSelect.add(option);
      });

      const materiaSelect = document.getElementById('materia');
      materiaSet.forEach(m => {
        const option = document.createElement('option');
        option.text = m;
        option.value = m;
        materiaSelect.add(option);
      });
    }

    function updateMateria() {
      // Completar tu lógica
    }

    function updateYear() {
      // Completar tu lógica
    }

    async function fetchFiles() {
      // Completar tu lógica
    }

    document.addEventListener('DOMContentLoaded', fetchSpreadsheet);
  </script>
</body>
</html>
